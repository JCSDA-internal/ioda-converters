# geovals.py
def gen_conv(ncin,ncout,clobber=True):
  """ gen_conv - generate GeoVaLs file for UFO
            for a specified GSI netCDF diag file
            for conventional observations

  Required arguments:
    ncin   - string path to input netCDF GSI diag file
    ncout  - string path to output GeoVaLs file
    
  Optional arguments:
    clobber - default True, if True will overwrite existing files
  """
  
def gen_rad(ncin,ncout,clobber=True):
  """ gen_rad - generate GeoVaLs file for UFO
            for a specified GSI netCDF diag file
            for radiances

  Required arguments:
    ncin   - string path to input netCDF GSI diag file
    ncout  - string path to output GeoVaLs file
    
  Optional arguments:
    clobber - default True, if True will overwrite existing files
  """
  
