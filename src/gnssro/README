----------There are 4 GNSSRO converters in this directory------

1. ------------------------------------------------------------
The converter implemented by gnssro_gsidiag2ioda.f90
It reads in the GNSSRO ncdiag files generated by GSI branch for jedi.
It  converts the ncdiag file to IODA file.
1) reorders the randomly distributed observations
   and output observations in profile record numbers.
2) adds variable descriptions including "long name", "unit", "valid_range", etc.
3) output observation and geoval(optional) files seperately.
Usage,
a) write obs and geoval
 gnssro_gsidiag2ioda.exe  yyyymmddhh $gsidiag_input 1
b) write obs only
 gnssro_gsidiag2ioda.exe  yyyymmddhh $gsidiag_input 0

2.-------------------------------------------------------------
The converter implemented by gnssro_bufr2ioda.f90
It reads in the GNSSRO Bufr files in NCEP operational Bufr format,
and converts the Bufr file to IODA file.

Usage:
  $builddir/bingnssro_bufr2ioda_generic yyyymmddhh $bufrfile_input $netcdffile_output (optional: t/T)

NOTE: the 4th argument should be set to true if one wants to apply extra sanity checks (EMC style)
      default is false; all those checks can be alternatively set in UFO obs filters,

3.-------------------------------------------------------------
gnssro_bufr2ioda.py
It reads individual RO profiles of WMO Bufr format and writes the IODA file.
please check the codes for details

4.-------------------------------------------------------------
gnssro_AWSopendataNetcdf2ioda.py
It reads individual RO profiles of NETCDF format and writes the IODA file.
please check the codes for details
